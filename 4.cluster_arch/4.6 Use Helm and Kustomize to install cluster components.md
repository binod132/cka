### Kustomization
K8s Documentation: https://kubernetes.io/docs/tasks/manage-kubernetes-objects/kustomization/
Killercoda: https://killercoda.com/chadmcrowell/course/cka/kustomize-apply
#### Demo: Customizing a Deployment with Kustomize
##### Create base manifests
```sh 
mkdir base
```
###### deployment.yaml
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
      - name: nginx
        image: nginx:1.25.0
```
###### service.yaml
```yaml
apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    run: web
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
  selector:
    run: web
```
##### Create base Kustomization file (base/kustomization.yaml):
```yaml
resources:
resources:
- deployment.yaml
- service.yaml
```

#### Create production overlay and patch
##### production/patch.yaml

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
spec:
  replicas: 3
---
apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    run: web
spec:
  ports:
  - name: https
    port: 8080
    protocol: TCP
  selector:
    run: web
```
##### Create my-app/overlays/production/kustomization.yaml
```yaml
resources:
- ../base
namePrefix: prod-
patches:
- path: patch.yaml
labels:
- pairs:
    env: prods
```
##### kubectl apply -k production/
Killercoda: https://killercoda.com/chadmcrowell/course/cka/helm-install-nginx