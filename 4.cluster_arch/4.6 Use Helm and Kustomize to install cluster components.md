### Kustomization
K8s Documentation: https://kubernetes.io/docs/tasks/manage-kubernetes-objects/kustomization/

Killercoda: 
- https://killercoda.com/chadmcrowell/course/cka/kustomize-apply
- https://killercoda.com/chadmcrowell/course/cka/kustomize-common-labels
- https://killercoda.com/chadmcrowell/course/cka/kustomize-configmap-secret
- https://killercoda.com/chadmcrowell/course/cka/kustomize-env-overlay
- https://killercoda.com/chadmcrowell/course/cka/kustomize-patch-image
#### Demo: Customizing a Deployment with Kustomize
##### Create base manifests
```sh 
mkdir base
```
###### deployment.yaml
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
      - name: nginx
        image: nginx:1.25.0
```
###### service.yaml
```yaml
apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    run: web
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
  selector:
    run: web
```
##### Create base Kustomization file (base/kustomization.yaml):
```yaml
resources:
- deployment.yaml
- service.yaml
```

#### Create production overlay and patch
##### production/patch.yaml

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
spec:
  replicas: 3
---
apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    run: web
spec:
  ports:
  - name: https
    port: 8080
    protocol: TCP
  selector:
    run: web
```
##### Create production/kustomization.yaml
```yaml
resources:
- ../base
namePrefix: prod-
patches:
- path: patch.yaml
labels:
- pairs:
    env: prods
```
##### kubectl apply -k production/

### Helm
Killercoda: https://killercoda.com/chadmcrowell/course/cka/helm-install-nginx

✅ Repository Management

Added and configured Helm repositories
Updated repository indexes
Searched for available charts
✅ Release Management

Installed Helm charts with custom configurations
Used both command-line flags and values files
Managed releases across different namespaces
✅ Upgrades and Configuration

Upgraded releases with new values
Combined values files with command-line overrides
Verified configuration changes
✅ Rollback and Troubleshooting

Performed rollback operations
Investigated failed deployments
Used troubleshooting commands effectively
Key Takeaways for CKA Exam
Always verify your context - Use kubectl config current-context
Use dry-run for validation - --dry-run --debug helps catch errors
Know your rollback options - Practice helm history and helm rollback
Understand values precedence - Command line > values file > chart defaults
Master troubleshooting commands - helm status , helm get values , kubectl describe