
###Note on kubeadm init full version must be defined on 

####create specific version like v1.34.1

### Note while install kubeadm, kubelet, kubectl fix version

### if already installed find version

kubelet --version
kubeadm version

### after adding key and repo
# find version
apt-cache madison kubeadm kubelet kubectl

# Unload if holed before
sudo apt-mark unhold kubelet kubeadm kubectl

# downgrade to required version if already installed 
#######
sudo apt install -y kubelet=1.34.1-1.1 kubeadm=1.34.1-1.1 kubectl=1.34.1-1.1 --allow-downgrades

## Installation
kubeadm init --help

sudo kubeadm init --kubernetes-version=v1.34.1 --pod-network-cidr=192.168.0.0/16 --ignore-preflight-errors=NumCPU --ignore-preflight-errors=Mem --dry-run

### (Optional)
sudo kubeadm init phase preflight

sudo kubeadm config images pull